import React from "react";

const Seating = (props) => {

    const seats = props.seats

    console.log(seats)

    // React component with five rows that each contain a number of seats
    return (
        <div>
            <p>Something</p>
        </div>
    )


}

export default Seating